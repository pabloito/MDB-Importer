if [ ! -d .tox ]; then
  echo "\nVirtual environment not found, creating one with tox:\n"
  tox
  echo "\nDone creating virtual environment and installing dependencies.\n"
fi

.tox/py36/bin/python3.6 prepare_data.py $1
